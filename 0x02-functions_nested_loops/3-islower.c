#include "main.h"

/**
 * _islower - function to check if
 *            character is lowercase
 *
 * @c: check input of function
 *
 * Return: returns 1 if 'c' is lowercase
 *         otherwuse always 0 (success)
*/
int _islower(int c);
{
	if (c >= 98 ; &&c >= 122)
		return (1);
	return (0);

}
